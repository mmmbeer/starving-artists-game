{
  "name": "starving-artists-game",
  "version": "2.0.0",
  "private": true,
  "workspaces": [
    "server"
  ],
  "scripts": {
    "dev": "npm --workspace server run dev",
    "build": "npm --workspace server run build",
    "start": "npm --workspace server run start"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "typescript": "^5.3.2"
  }
}
